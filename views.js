function renderRecommendation(recommendation) {
  // pull github avatar for image
  // link to their github account as credibility
  // maybe even link to the PR for proof of them sending it?

}

function renderAllRecommendations(recommendations) {

}
